# Production Environment Variables for Railway

# Node Environment
NODE_ENV=production
PORT=5000

# Database URL (Railway will provide this automatically)
# Format: postgresql://user:password@host:port/database
DATABASE_URL=${{Postgres.DATABASE_URL}}

# JWT Configuration
# IMPORTANT: Generate secure random strings for production!
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING_64_CHARS
JWT_REFRESH_SECRET=CHANGE_THIS_TO_DIFFERENT_SECURE_RANDOM_STRING_64_CHARS
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# OTP Configuration
OTP_EXPIRES_IN=10

# CORS Configuration
# Update with your Railway frontend domains after deployment
CORS_ORIGIN=https://admin-winadeal.up.railway.app,https://vendor-winadeal.up.railway.app,https://customer-winadeal.up.railway.app,https://delivery-winadeal.up.railway.app

# Optional: Payment Gateway (if using)
# RAZORPAY_KEY_ID=your_razorpay_key
# RAZORPAY_KEY_SECRET=your_razorpay_secret

# Optional: Email Service (if using)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Optional: SMS Service (if using)
# TWILIO_ACCOUNT_SID=your_twilio_sid
# TWILIO_AUTH_TOKEN=your_twilio_token
# TWILIO_PHONE_NUMBER=+1234567890
